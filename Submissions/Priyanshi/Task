<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ZeroTheory - Explore the Cosmos of Innovation</title>
    <style>
        
        :root {
            --bg-color: #0a0a0a; 
            --text-color: #e0e0e0; 
            --accent-color: #00d4ff; 
            --header-bg: #1a1a2e; 
            --card-bg: #16213e; 
            --shadow: rgba(0, 212, 255, 0.2); 
            --star-opacity: 1; 
        }

        [data-theme="earth"] {
            --bg-color: #ffffff;
            --text-color: #333333;
            --accent-color: #007bff;
            --header-bg: #f8f9fa;
            --card-bg: #ffffff;
            --shadow: rgba(0, 0, 0, 0.1);
            --star-opacity: 0; 
        }

        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--bg-color);
            color: var(--text-color);
            transition: background-color 0.3s, color 0.3s;
            overflow-x: hidden;
            position: relative;
        }

        
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: radial-gradient(2px 2px at 20px 30px, #ffffff, transparent),
                              radial-gradient(2px 2px at 40px 70px, #ffffff, transparent),
                              radial-gradient(1px 1px at 90px 40px, #ffffff, transparent),
                              radial-gradient(1px 1px at 130px 80px, #ffffff, transparent),
                              radial-gradient(2px 2px at 160px 30px, #ffffff, transparent);
            background-repeat: repeat;
            background-size: 200px 100px;
            opacity: var(--star-opacity);
            transition: opacity 0.5s ease-in-out; 
            pointer-events: none;
            z-index: -1;
        }

        header {
            background-color: var(--header-bg);
            padding: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 2px 5px var(--shadow);
            position: sticky;
            top: 0;
            z-index: 100;
            transition: background-color 0.3s, box-shadow 0.3s;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--accent-color);
            text-shadow: 0 0 10px var(--accent-color);
            transition: color 0.3s, text-shadow 0.3s;
        }

        nav {
            display: flex;
            align-items: center;
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 1rem;
            margin: 0;
            padding: 0;
            transition: opacity 0.3s, visibility 0.3s;
        }

        nav.hidden ul {
            opacity: 0;
            visibility: hidden;
        }

        nav a {
            text-decoration: none;
            color: var(--text-color);
            transition: color 0.3s;
        }

        nav a:hover {
            color: var(--accent-color);
            text-shadow: 0 0 5px var(--accent-color);
        }

        #nav-toggle {
            background: none;
            border: none;
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--text-color);
            margin-left: 1rem;
            transition: color 0.3s;
        }

        #theme-toggle {
            background: none;
            border: none;
            font-size: 1.5rem;
            cursor: pointer;
            color: var(--text-color);
            transition: color 0.3s;
        }

        .hero {
            text-align: center;
            padding: 4rem 2rem;
            background: linear-gradient(135deg, #0f0f23, #1a1a2e, #16213e); /* Cosmic gradient */
            color: white;
            position: relative;
            overflow: hidden;
            animation: fadeIn 1s ease-in;
            transition: background 0.3s;
        }

        [data-theme="earth"] .hero {
            background: linear-gradient(135deg, #87ceeb, #98fb98, #f0e68c); 
            color: #333333;
        }

        .hero::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="50" cy="50" r="2" fill="white"/><circle cx="20" cy="30" r="1" fill="white"/><circle cx="80" cy="70" r="1.5" fill="white"/></svg>');
            background-repeat: repeat;
            background-size: 100px 100px;
            opacity: var(--star-opacity);
            transition: opacity 0.5s ease-in-out;
            pointer-events: none;
        }

        .hero h1 {
            font-size: 3rem;
            margin: 0;
            text-shadow: 0 0 20px var(--accent-color);
            transition: text-shadow 0.3s;
        }

        .hero p {
            font-size: 1.2rem;
            margin: 1rem 0;
        }

        .btn {
            background-color: var(--accent-color);
            color: #0a0a0a;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
            transition: transform 0.3s, box-shadow 0.3s, background-color 0.3s, color 0.3s;
            box-shadow: 0 0 10px var(--accent-color);
        }

        [data-theme="earth"] .btn {
            background-color: var(--accent-color);
            color: white;
        }

        .btn:hover {
            transform: scale(1.05);
            box-shadow: 0 0 20px var(--accent-color);
        }

        .features {
            padding: 4rem 2rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .feature-card {
            background-color: var(--card-bg);
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 8px var(--shadow);
            text-align: center;
            transition: transform 0.3s, box-shadow 0.3s, background-color 0.3s, border-color 0.3s;
            border: 1px solid var(--accent-color);
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 16px var(--shadow);
        }

        .feature-card h3 {
            color: var(--accent-color);
            text-shadow: 0 0 10px var(--accent-color);
            transition: color 0.3s, text-shadow 0.3s;
        }

        footer {
            text-align: center;
            padding: 2rem;
            background-color: var(--header-bg);
            color: var(--text-color);
            transition: background-color 0.3s, color 0.3s;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .fade-in {
            animation: fadeIn 1s ease-in;
        }

        /* Floating Animation for Stars or Elements */
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        .floating {
            animation: float 3s ease-in-out infinite;
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2rem;
            }
            nav ul {
                flex-direction: column;
                gap: 0.5rem;
                position: absolute;
                top: 100%;
                left: 0;
                width: 100%;
                background-color: var(--header-bg);
                padding: 1rem;
                box-shadow: 0 2px 5px var(--shadow);
            }
            nav.hidden ul {
                display: none;
            }
        }
    </style>
</head>
<body data-theme="space">
    <header>
        <div class="logo">ZeroTheory</div>
        <nav id="nav">
            <ul>
                <li><a href="#home">Home</a></li>
                <li><a href="#features">Features</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
            <button id="nav-toggle">☰</button>
        </nav>
        <button id="theme-toggle">🌍</button>
    </header>

    <section id="home" class="hero">
        <h1 class="floating">Explore the Cosmos of Innovation</h1>
        <p>ZeroTheory: Where AI meets the infinite possibilities of the universe.</p>
        <button class="btn">Launch Journey</button>
    </section>

    <section id="features" class="features">
        <div class="feature-card fade-in">
            <h3>Galactic AI Insights</h3>
            <p>Harness cosmic-level AI to unlock deep, universe-spanning insights and decisions.</p>
        </div>
        <div class="feature-card fade-in">
            <h3>Interstellar Integration</h3>
            <p>Seamlessly integrate ZeroTheory into your workflows, bridging worlds for efficiency.</p>
        </div>
        <div class="feature-card fade-in">
            <h3>Cosmic Community</h3>
            <p>Join explorers across the galaxy, sharing ideas and collaborating on stellar projects.</p>
        </div>
    </section>

    <footer>
        <p>&copy; 2023 ZeroTheory. All rights reserved. | Exploring the Final Frontier</p>
    </footer>

    <script>

        const themeToggle = document.getElementById('theme-toggle');
        const body = document.body;

        themeToggle.addEventListener('click', () => {
            const currentTheme = body.getAttribute('data-theme');
            const newTheme = currentTheme === 'space' ? 'earth' : 'space';
            body.setAttribute('data-theme', newTheme);
            themeToggle.textContent = newTheme === 'earth' ? '🚀' : '🌍';
        });

        
        const navToggle = document.getElementById('nav-toggle');
        const nav = document.getElementById('nav');

        navToggle.addEventListener('click', () => {
            nav.classList.toggle('hidden');
        });

        
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('fade-in');
                }
            });
        });

        document.querySelectorAll('.feature-card').forEach(card => {
            observer.observe(card);
        });

        
        document.querySelector('.btn').addEventListener('click', () => {
            alert('Prepare for launch! ZeroTheory is ready to explore.');
        });

        
        function createStar() {
            if (body.getAttribute('data-theme') === 'space') {
                const star = document.createElement('div');
                star.style.position = 'absolute';
                star.style.width = '2px';
                star.style.height = '2px';
                star.style.backgroundColor = 'white';
                star.style.left = Math.random() * 100 + 'vw';
                star.style.top = Math.random() * 100 + 'vh';
                star.style.animation = 'float 3s ease-in-out infinite';
                document.body.appendChild(star);
                setTimeout(() => star.remove(), 3000);
            }
        }

        setInterval(createStar, 1000); 
    </script>
</body>
</html>
